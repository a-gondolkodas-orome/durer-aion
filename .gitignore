# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
**/node_modules
**/build
/.pnp
.pnp.js

# testing
/coverage
**/coverage

# production
/build
/server

# misc
.DS_Store
.env.local
.env.docker
.env.development.local
.env.test.local
.env.production.local
.env

npm-debug.log*
yarn-debug.log*
yarn-error.log*
scripts/*.tsv*
__pycache__
.env
.venv
.turbo

**/dist

# The Pages tree the deploy assembles from pages/ and each app's dist/.
/site

# `npm run report:outdated -- --out dependency-report.md` output
/dependency-report.md

# Per-machine compose overrides, e.g. the TLS one in DEPLOYMENT.md § 8: they name
# that host's certificate and belong to no other.
/docker-compose.*.yml
!/docker-compose.dev.yml
