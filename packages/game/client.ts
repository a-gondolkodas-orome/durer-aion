// The `game/client` entry: the strategy games' React boards and descriptions.
// The frontends import it; the server, which never renders a board, does not.
export { MyBoardWrapper, descriptionC, descriptionD, descriptionE } from './src/games/strategy/strategy-client';
